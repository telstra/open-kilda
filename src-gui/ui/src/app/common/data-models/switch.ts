import { NULL_INJECTOR } from "@angular/core/src/render3/component";

export interface Switch {
    switch_id: string;
    commonname : string;
    name: string;
    address: string;
    hostname: string;
    pop_location: string;
    description: string;
    state:string;
}
import { SwitchidmaskPipe } from './switchidmask.pipe';

describe('SwitchidmaskPipe', () => {
  it('create an instance', () => {
    const pipe = new SwitchidmaskPipe();
    expect(pipe).toBeTruthy();
  });
});

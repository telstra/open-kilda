import { NULL_INJECTOR } from "@angular/core/src/render3/component";

export interface Switch {
    switch_id: string;
    name: string;
    address: string;
    hostname: string;
    description: string;
    state:string;
}